import 'package:flutter/material.dart';

class SaudeEBemEstarScreen extends StatelessWidget {
  const SaudeEBemEstarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Aqui estão a saude e bem estar',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}