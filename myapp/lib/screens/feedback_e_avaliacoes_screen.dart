import 'package:flutter/material.dart';

class FeedbackEAvaliacoesScreen extends StatelessWidget {
  const FeedbackEAvaliacoesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Aqui estão os feedbacks e avaliações',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}