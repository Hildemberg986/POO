target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # flutter_application_path = File.join('..', '..')  # Caminho relativo correto ao seu projeto Flutter
  # load File.join(flutter_application_path, '.flutter-plugins-dependencies.rb')  
end
