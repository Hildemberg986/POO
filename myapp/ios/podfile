target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_application_path = File.join('flutter')  # Caminho relativo a partir de onde o Podfile está localizado
  load File.join(flutter_application_path, 'flutter_export_ios_plugins.rb')
end
