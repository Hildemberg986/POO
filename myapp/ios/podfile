platform :ios, '12.0'

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  load File.join('..', 'Flutter', 'flutter_export_ios_plugins')

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end
